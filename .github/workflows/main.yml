name: Build Template

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:

  prebuild:
    steps:
      - name: Display echo file
        run: echo "Build Template"

  pre-prod:
    steps:
      - name: Display preprod
        run: echo "Pre production deploy"
